# 通信販売システム ユーザーマニュアル

## 概要
このシステムは、商品の検索・注文・在庫管理・顧客管理を行う本格的な通信販売システムです。
フロントエンド（React）とバックエンド（Node.js + SQLite）で構成されています。

---

## システム起動方法

### 1. バックエンドサーバー起動
```bash
cd backend
node server2.js
```

**起動確認**: コンソールに以下が表示されること
```
既存DBに接続成功
Productsテーブル準備完了
Stocksテーブル準備完了
Customersテーブル準備完了
Ordersテーブル準備完了
OrderItemsテーブル準備完了
サーバー起動: http://localhost:3001
```

### 2. フロントエンド起動
```bash
cd frontend/src
npm start
```

**アクセス**: ブラウザで `http://localhost:3000` を開く

---

## 機能説明

### 📦 商品管理機能

#### 1. 商品追加（新商品登録）
**場所**: 「商品検索・注文」セクションの上部、グレーのボックス

**手順**:
1. 商品ID（数字）を入力
2. 商品名を入力
3. 価格（数字）を入力
4. 在庫数を入力（省略可、デフォルト100）
5. 「商品追加」ボタン（緑）をクリック

**例**:
- 商品ID: `2`
- 商品名: `Tシャツ`
- 価格: `1500`
- 在庫数: `50`

#### 2. 既存データ移行
**場所**: 「TestTableデータを商品DBに移行」ボタン（オレンジ）

**用途**: 
- 古いTestTableのデータを新しい商品システムに移行
- 初回セットアップ時に使用

---

### 🔍 商品検索機能

#### 検索方法
1. 検索ボックスに商品名を入力
2. 「検索」ボタンをクリック
3. 検索結果が表形式で表示

**表示情報**:
- 商品ID
- 商品名  
- 価格
- 在庫数
- 操作ボタン（カートに追加）

---

### 🛒 ショッピングカート機能

#### カートへの追加
1. 商品検索結果で「カートに追加」ボタンをクリック
2. カート内容が画面上部に表示

#### カート操作
- **数量変更**: 数量入力欄で変更
- **商品削除**: 「削除」ボタンをクリック
- **合計確認**: 自動で合計金額が計算される

---

### 📋 注文処理機能

#### 注文フロー
1. **商品選択**: カートに商品を追加
2. **注文開始**: 「注文に進む」ボタンをクリック
3. **顧客情報入力**: モーダルフォームで以下を入力
   - お名前
   - 住所
   - 連絡先
4. **支払い方法選択**: 以下から選択
   - 銀行振込
   - コンビニ決済
   - 代金引換
   - クレジットカード
5. **注文確認**: 注文内容と合計金額を確認
6. **注文確定**: 「注文確定」ボタンをクリック

#### 注文完了
- 注文番号が表示される
- カートが自動でクリアされる
- 在庫が自動で確認される

---

### 🛠️ 管理機能（API経由）

#### 在庫管理
**在庫確認**: `GET http://localhost:3001/api/stocks`
**在庫更新**: `PUT http://localhost:3001/api/products/:id/stock`

#### 注文管理
**注文一覧**: `GET http://localhost:3001/api/orders`
**未発送注文**: `GET http://localhost:3001/api/orders?status=unshipped`
**注文詳細**: `GET http://localhost:3001/api/orders/:id`

#### 顧客管理
**顧客一覧**: `GET http://localhost:3001/api/customers`
**顧客詳細**: `GET http://localhost:3001/api/customers/:id`

---

## データベース構造

### 主要テーブル
- **Products**: 商品マスタ（ID、名前、価格）
- **Stocks**: 在庫情報（商品ID、実在庫、有効在庫）
- **Customers**: 顧客情報（ID、名前、住所、連絡先）
- **Orders**: 注文データ（注文ID、顧客ID、支払・発送状態）
- **OrderItems**: 注文詳細（注文ID、商品ID、数量、価格）

---

## トラブルシューティング

### よくある問題

#### 1. 商品検索で何も表示されない
**原因**: 商品が登録されていない
**解決**: 
- 「新商品追加」で商品を登録
- または「TestTableデータを商品DBに移行」を実行

#### 2. 注文でエラーが発生
**原因**: 在庫不足または顧客情報不備
**解決**:
- 在庫数を確認
- 顧客情報（名前、住所、連絡先）をすべて入力

#### 3. サーバー接続エラー
**原因**: バックエンドサーバーが起動していない
**解決**: `cd backend && node server2.js` でサーバーを起動

#### 4. 環境変数エラー
**原因**: REACT_APP_API_URLが未設定
**解決**: `frontend/src/.env` ファイルを確認
```
REACT_APP_API_URL=http://localhost:3001
```

---

## 動作確認手順

### 基本フロー確認
1. ✅ サーバー起動（backend & frontend）
2. ✅ 商品追加（ID:2, 名前:Tシャツ, 価格:1500）
3. ✅ 商品検索（「Tシャツ」で検索）
4. ✅ カート追加（「カートに追加」ボタン）
5. ✅ 注文処理（顧客情報入力→注文確定）
6. ✅ 注文完了（注文番号表示）

### API動作確認
```bash
# 商品一覧確認
curl http://localhost:3001/api/products?q=

# 在庫確認  
curl http://localhost:3001/api/stocks

# 注文一覧確認
curl http://localhost:3001/api/orders
```

---

## システム特徴

### 🔒 セキュリティ機能
- トランザクション処理でデータ整合性保証
- 外部キー制約による参照整合性
- 入力値検証とエラーハンドリング

### ⚡ パフォーマンス
- SQLiteによる高速データアクセス
- RESTful API設計
- React Hooksによる効率的な状態管理

### 🎯 ビジネス機能
- 完全な注文フロー実装
- 在庫管理（実在庫・有効在庫）
- 支払・発送状態管理
- 顧客情報管理

---

## 今後の拡張予定

1. **認証機能** - ユーザーログイン・権限管理
2. **決済連携** - クレジットカード決済API
3. **メール通知** - 注文確認・発送通知
4. **管理画面** - 注文・在庫・顧客の管理UI
5. **レポート機能** - 売上・在庫分析
6. **PDF出力** - 請求書・納品書生成

---

**📞 サポート**: システムに関する質問は開発チームまでお問い合わせください。