# 管理者システム操作マニュアル

## 概要

飲食店向け通信販売システムの管理者向け操作マニュアルです。本システムは4つの専門管理画面と統合バックエンドAPIで構成され、E-Commerceビジネスの全業務フローを管理できます。

## システム構成

### 管理システム一覧

| システム名 | ポート | URL | 機能概要 |
|-----------|-------|-----|---------|
| **メイン管理システム** | 3004 | http://localhost:3004 | 商品管理・統計・システム管理 |
| **注文管理システム** | 3001 | http://localhost:3001 | 注文ステータス・顧客管理 |
| **経理管理システム** | 3002 | http://localhost:3002 | 支払い管理・売上分析 |
| **配送管理システム** | 3003 | http://localhost:3003 | 配送ステータス・追跡管理 |
| **バックエンドAPI** | 3005 | http://localhost:3005 | 統合データベースAPI |

---

## 1. メイン管理システム (Port 3004)

### 🏠 ダッシュボード機能

**アクセス**: http://localhost:3004

#### システム統計表示
- **総商品数**: 登録されている商品の総数
- **総注文数**: システム内の全注文件数
- **総売上**: 累計売上金額
- **アクティブユーザー数**: 利用中のユーザー数

#### 基本操作
```
1. ブラウザでhttp://localhost:3004にアクセス
2. ダッシュボードで全体状況を確認
3. 各管理機能にナビゲート
```

### 📦 商品管理機能

#### プレミアム商品管理
**目的**: メイン商品カタログ（Products/Stocks）の管理

**操作手順**:
1. **新商品追加**
   - 「プレミアム商品を追加」セクションを利用
   - 商品名、価格、在庫数量を入力
   - 「追加」ボタンをクリック
   - ✅ Products テーブルと Stocks テーブルに同時登録

2. **商品画像アップロード**
   - 商品ID を確認
   - ファイル選択で画像ファイル（JPG推奨）を選択
   - 「画像アップロード」ボタンをクリック
   - 📁 画像は `backend/public/images/{商品ID}.jpg` に保存

#### TestTable商品管理（レガシー）
**目的**: 開発・テスト用の既存商品データ管理

**操作手順**:
1. **インライン編集**
   - テーブル内の商品名・価格セルをクリック
   - 直接編集して Enter キーで保存
   - ❌ 削除は「削除」ボタンをクリック

2. **新規追加**
   - 「新しい商品を追加」フォームを使用
   - ID、商品名、価格を入力して追加

#### データ移行機能
**目的**: TestTable → Products/Stocks への本格移行

**操作手順**:
```
1. 「データ移行実行」ボタンをクリック
2. TestTable のデータが Products/Stocks に複製
3. 在庫数量は初期値（100）で設定
4. 移行完了メッセージを確認
```

---

## 2. 注文管理システム (Port 3001)

### 📋 注文管理機能

**アクセス**: http://localhost:3001

#### 注文ステータス管理
**利用可能ステータス**:
- `pending`: 注文確認中
- `confirmed`: 注文確定
- `shipped`: 発送済み
- `delivered`: 配送完了

#### 基本操作

1. **注文一覧表示**
   ```
   - 全注文がリスト表示
   - 注文ID、顧客情報、ステータス、金額を表示
   - リアルタイム更新
   ```

2. **注文検索・フィルタリング**
   ```
   - ステータス別フィルタ: ドロップダウンで選択
   - 顧客名検索: テキスト入力で部分一致検索
   - 注文ID検索: 数値入力で完全一致検索
   ```

3. **注文詳細管理**
   ```
   - 注文詳細の確認・編集
   - ステータス更新と備考追加
   - 顧客情報の表示・編集
   ```

#### ワークフロー例

```
[新規注文受付]
1. 注文ステータス: pending で受付
2. 注文内容を確認・検証
3. ステータスを confirmed に更新
4. 備考欄に確認事項を記録

[発送処理]
1. 商品準備完了後 shipped に更新
2. 追跡番号等を備考に記録
3. 顧客への発送通知

[配送完了]
1. 配送確認後 delivered に更新
2. 完了日時を記録
```

---

## 3. 経理管理システム (Port 3002)

### 💰 支払い・売上管理機能

**アクセス**: http://localhost:3002

#### 支払いステータス管理
**利用可能ステータス**:
- `pending`: 支払い待ち
- `completed`: 支払い完了
- `failed`: 支払い失敗
- `refunded`: 返金済み

#### 基本操作

1. **支払い一覧表示**
   ```
   - 支払いID、注文ID、金額、支払い方法、ステータス
   - 支払い日時と更新履歴
   - ステータス別の色分け表示
   ```

2. **売上分析機能**
   ```
   - 期間別売上集計（本日・今週・今月）
   - 支払い方法別分析
   - ステータス別集計
   - 売上推移グラフ
   ```

3. **支払い処理ワークフロー**
   ```
   [支払い確認]
   1. pending 状態の支払いを確認
   2. 決済システムでの入金確認
   3. ステータスを completed に更新
   
   [問題対応]
   1. 支払い失敗時は failed に更新
   2. 返金処理時は refunded に変更
   3. 各処理で備考を詳細記録
   ```

#### 財務レポート機能

```
[日次レポート]
- 本日の売上合計
- 支払い完了件数
- 未処理支払い件数

[週次・月次レポート]
- 期間売上推移
- 支払い方法別統計
- 返金・失敗率の分析
```

---

## 4. 配送管理システム (Port 3003)

### 🚚 配送・物流管理機能

**アクセス**: http://localhost:3003

#### 配送ステータス管理
**利用可能ステータス**:
- `preparing`: 配送準備中
- `in_transit`: 配送中
- `delivered`: 配送完了
- `returned`: 返送済み

#### 基本操作

1. **配送一覧表示**
   ```
   - 配送ID、注文ID、配送先住所
   - 配送ステータスと優先度
   - 追跡番号と配送業者情報
   ```

2. **配送スケジュール管理**
   ```
   - 配送予定日の設定・更新
   - 優先度設定（高・中・低）
   - 配送ルートの最適化情報
   ```

3. **追跡情報管理**
   ```
   - 追跡番号の登録・更新
   - 配送業者の選択・変更
   - リアルタイム配送状況更新
   ```

#### 配送処理ワークフロー

```
[配送準備]
1. preparing ステータスで配送準備開始
2. 商品ピッキング・梱包
3. 配送業者・追跡番号を登録

[配送開始]
1. in_transit に変更
2. 顧客に追跡番号を通知
3. 配送進捗を定期更新

[配送完了]
1. delivered に更新
2. 配送完了日時を記録
3. 顧客満足度調査の送信
```

#### 配送分析機能

```
[配送効率分析]
- 地域別配送時間
- 配送業者別パフォーマンス
- 配送コスト分析

[問題追跡]
- 遅延配送の分析
- 返送率の監視
- 顧客満足度の追跡
```

---

## 5. バックエンドAPI管理 (Port 3005)

### 🔧 システム管理・API機能

**アクセス**: http://localhost:3005

#### 利用可能なAPI エンドポイント

##### 商品管理API
```http
GET    /api/products              # 商品一覧取得
POST   /api/products              # 新商品追加
GET    /api/TestTable             # TestTable商品一覧
POST   /api/TestTable             # TestTable商品追加
PUT    /api/TestTable/:id         # TestTable商品更新
DELETE /api/TestTable/:id         # TestTable商品削除
```

##### 注文管理API
```http
GET    /api/orders                # 注文一覧取得
POST   /api/orders                # 新規注文作成
PUT    /api/orders/:id            # 注文情報更新
GET    /api/orders?status=pending # ステータス別フィルタ
```

##### 顧客管理API
```http
GET    /api/customers             # 顧客一覧取得
POST   /api/customers             # 新規顧客登録
PUT    /api/customers/:id         # 顧客情報更新
DELETE /api/customers/:id         # 顧客削除
```

##### 在庫管理API
```http
GET    /api/stocks                # 在庫一覧取得
PUT    /api/stocks/:id            # 在庫数量更新
```

##### 支払い管理API
```http
GET    /api/payments              # 支払い一覧取得
PUT    /api/payments/:id          # 支払いステータス更新
```

##### 配送管理API
```http
GET    /api/shipments             # 配送一覧取得
PUT    /api/shipments/:id         # 配送ステータス更新
```

##### システム統計API
```http
GET    /api/stats                 # ダッシュボード統計取得
GET    /health                    # システムヘルスチェック
```

##### ファイル管理API
```http
POST   /api/upload                # 画像ファイルアップロード
GET    /images/:filename          # 画像ファイル配信
```

##### データ管理API
```http
POST   /api/migrate-data          # TestTable→Products/Stocks移行
```

---

## 6. データベース管理

### 📊 データベース構造

#### メインテーブル
```sql
-- 商品マスター
Products (ProductID, Name, Price)

-- 在庫管理
Stocks (ProductID, StockQuantity1, StockQuantity2)

-- 注文管理
Orders (OrderID, CustomerID, TotalAmount, Status, OrderDate)
OrderItems (OrderItemID, OrderID, ProductID, Quantity, UnitPrice)

-- 顧客管理
Customers (CustomerID, Name, Email, Phone, Address)

-- 支払い管理
Payments (PaymentID, OrderID, Amount, PaymentMethod, Status, PaymentDate)

-- 配送管理
Shipments (ShipmentID, OrderID, TrackingNumber, Status, ShippedDate, DeliveredDate)

-- レガシーテーブル
TestTable (ID, Name, Price)
```

#### データ整合性管理
```
- トランザクション処理による安全な更新
- 外部キー制約による参照整合性
- 在庫数量の自動計算・更新
- 注文ステータスの連動更新
```

---

## 7. システム起動・停止手順

### 🚀 システム起動

#### Docker環境での起動
```bash
# プロジェクトディレクトリに移動
cd software_dev

# Dockerコンテナ起動
docker-compose up -d

# 起動確認
docker-compose ps
```

#### 手動起動（開発環境）
```bash
# バックエンドAPI起動
cd backend
node server2.js

# 各管理システム起動（別ターミナルで実行）

# メイン管理システム
cd admin-management
npm start

# 注文管理システム
cd order-management
npm start

# 経理管理システム
cd accounting-management
npm start

# 配送管理システム
cd shipping-management
npm start
```

### 🛑 システム停止

#### Docker環境での停止
```bash
# 全コンテナ停止
docker-compose down

# データも削除する場合
docker-compose down -v
```

#### 手動停止
```bash
# 各プロセスをCtrl+Cで停止
```

---

## 8. 運用・保守

### 📈 日常運用タスク

#### 毎日実行する作業
```
1. システムヘルスチェック
   - http://localhost:3005/health でAPI稼働確認
   - 各管理画面のアクセス確認

2. 注文処理
   - 新規注文（pending）の確認・承認
   - 支払い確認と処理
   - 配送準備と発送処理

3. 在庫管理
   - 在庫数量の確認
   - 低在庫商品の補充手配
   - 商品マスターの更新

4. 売上分析
   - 日次売上の確認
   - 支払い状況の監視
   - 異常取引の検出
```

#### 週次・月次作業
```
1. データバックアップ
   - SQLiteデータベースのバックアップ
   - 画像ファイルのバックアップ

2. パフォーマンス分析
   - 売上傾向の分析
   - 顧客行動の分析
   - システムパフォーマンスの監視

3. レポート作成
   - 売上レポート
   - 在庫レポート
   - 顧客満足度レポート
```

### 🔧 トラブルシューティング

#### よくある問題と解決方法

##### 1. システムアクセス不可
```
症状: 管理画面にアクセスできない
原因: サーバー未起動、ポート競合
解決:
1. docker-compose ps でコンテナ状況確認
2. docker-compose restart で再起動
3. ブラウザキャッシュクリア
```

##### 2. データ更新エラー
```
症状: 商品・注文情報が更新されない
原因: データベース接続エラー、トランザクション失敗
解決:
1. /health エンドポイントでAPI状況確認
2. ブラウザの開発者ツールでエラー詳細確認
3. データベースファイルの権限確認
```

##### 3. 画像アップロード失敗
```
症状: 商品画像がアップロードできない
原因: ファイルサイズ、権限、ディスク容量
解決:
1. backend/public/images/ ディレクトリの権限確認
2. ファイルサイズ制限の確認（通常10MB以下）
3. ディスク容量の確認
```

##### 4. レスポンス遅延
```
症状: 管理画面の動作が遅い
原因: データベース負荷、メモリ不足
解決:
1. システムリソース（CPU、メモリ）の確認
2. データベースファイルサイズの確認
3. 不要なデータの削除・アーカイブ
```

---

## 9. セキュリティ・注意事項

### ⚠️ 重要な制限事項

#### 認証・認可
```
❌ 本システムには認証機能がありません
❌ 管理画面に直接URLでアクセス可能
❌ APIエンドポイントに認証なしでアクセス可能
❌ ユーザー権限管理なし
```

#### セキュリティ対策
```
🔒 本番環境での使用時の必須対策:
1. VPNまたはファイアウォールでアクセス制限
2. HTTPSの実装
3. 認証システムの実装
4. APIアクセストークンの実装
5. 入力データの検証・サニタイゼーション
```

#### データ保護
```
📊 データベース保護:
1. 定期的なバックアップ
2. データベースファイルのアクセス権限制限
3. 機密情報の暗号化
4. 監査ログの実装
```

---

## 10. 管理業務フロー例

### 📋 1日の管理業務フロー

#### 朝の開始作業（9:00-10:00）
```
1. システム起動確認
   - 全管理画面のアクセステスト
   - APIヘルスチェック

2. 新規注文の確認
   - 注文管理システムで pending 注文を確認
   - 注文内容の検証・承認
   - ステータスを confirmed に更新

3. 支払い確認
   - 経理管理システムで未処理支払いを確認
   - 決済システムとの照合
   - 支払い完了処理
```

#### 日中の運用作業（10:00-17:00）
```
1. 在庫管理
   - 在庫数量の監視
   - 低在庫アラートの対応
   - 商品補充の手配

2. 配送処理
   - 配送管理システムで preparing を確認
   - 配送業者への発送依頼
   - 追跡番号の登録
   - ステータス更新（in_transit）

3. 顧客対応
   - 注文問い合わせの対応
   - 配送状況の確認・報告
   - 返品・交換処理
```

#### 夕方の終了作業（17:00-18:00）
```
1. 売上確認
   - 経理管理システムで日次売上確認
   - 支払い状況の最終チェック
   - 売上レポートの作成

2. 配送状況確認
   - 配送中商品の状況確認
   - 遅延配送の原因調査・対応
   - 翌日配送予定の準備

3. システム終了処理
   - データバックアップ
   - システムログの確認
   - 翌日作業の準備

4. 日報作成
   - 売上実績
   - 注文処理件数
   - 配送完了件数
   - 問題発生状況
```

---

## 11. FAQ（よくある質問）

### Q1: 新しい商品を追加するにはどうすればよいですか？
**A**: メイン管理システム（port 3004）の「プレミアム商品を追加」セクションで商品名、価格、在庫数量を入力して追加してください。同時に画像のアップロードも可能です。

### Q2: 注文ステータスを一括で変更できますか？
**A**: 現在のシステムでは個別の注文ごとにステータス更新が必要です。注文管理システムで各注文を選択して個別に処理してください。

### Q3: 売上レポートはどこで確認できますか？
**A**: 経理管理システム（port 3002）で期間別（本日・今週・今月）の売上分析とレポートを確認できます。

### Q4: 在庫が不足した場合の通知機能はありますか？
**A**: 現在は手動での在庫確認が必要です。メイン管理システムで定期的に在庫数量を確認してください。

### Q5: データのバックアップはどのように行いますか？
**A**: `backend/Inshokuten.sqlite3` ファイルと `backend/public/images/` フォルダを定期的にコピーしてバックアップしてください。

### Q6: システムが重くなった場合の対処法は？
**A**: 
1. 不要なブラウザタブを閉じる
2. データベースファイルサイズを確認
3. システムリソース（CPU・メモリ）を確認
4. 必要に応じてシステム再起動

### Q7: 商品画像が表示されない場合は？
**A**: 
1. 画像ファイルが `backend/public/images/{商品ID}.jpg` に存在するか確認
2. ファイルの権限を確認
3. ブラウザキャッシュをクリア

---

## 12. まとめ

本管理システムは、E-Commerceビジネスの主要業務を効率的に管理するため、商品管理・注文処理・決済管理・配送管理の4つの専門システムと統合APIで構成されています。

### ✅ システムの長所
- **専門性**: 各業務に特化した管理画面
- **統合性**: 共通データベースによる一元管理  
- **効率性**: リアルタイム更新と直感的操作
- **拡張性**: APIベースの柔軟な機能拡張

### ⚠️ 運用時の注意点
- **セキュリティ**: 認証機能がないため、アクセス制限が必要
- **バックアップ**: 定期的なデータバックアップが必須
- **監視**: システム稼働状況の継続的な監視が重要

### 🚀 今後の改善提案
- ユーザー認証・認可システムの実装
- 在庫低下アラート機能の追加
- 売上予測・分析機能の強化
- モバイル対応の管理画面開発
- 監査ログ・セキュリティ機能の実装

---

**本マニュアルを参考に、効率的で安全なシステム運用を実現してください。**

---

*作成日: 2025年7月25日*  
*バージョン: 1.0*  
*対象システム: 飲食店向け通信販売システム*